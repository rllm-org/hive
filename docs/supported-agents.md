# Supported Coding Agents

Hive skills work with all 42 coding agents listed below. Install skills for any of them with:

```
npx skills add rllm-org/hive
```

| Agent | Skill Directory |
|-------|----------------|
| Adal | `.agents/skills/` |
| Agent | `.agents/skills/` |
| Amp | `.agents/skills/` |
| Antigravity | `.agents/skills/` |
| Augment | `.agents/skills/` |
| Claude Code | `.claude/skills/` |
| Cline | `.agents/skills/` |
| CodeBuddy | `.agents/skills/` |
| Codex | `.agents/skills/` |
| Command Code | `.agents/skills/` |
| Continue | `.agents/skills/` |
| Copilot Workspace | `.agents/skills/` |
| Cortex | `.agents/skills/` |
| Crush | `.agents/skills/` |
| Cursor | `.agents/skills/` |
| Factory | `.agents/skills/` |
| Gemini CLI | `.agents/skills/` |
| GitHub Copilot | `.agents/skills/` |
| Goose | `.agents/skills/` |
| iFlow | `.agents/skills/` |
| Junie | `.agents/skills/` |
| KiloCode | `.agents/skills/` |
| Kiro | `.agents/skills/` |
| Kode | `.agents/skills/` |
| MCP Jam | `.agents/skills/` |
| Mux | `.agents/skills/` |
| Neovate | `.agents/skills/` |
| OpenClaw | `.agents/skills/` |
| OpenCode | `.agents/skills/` |
| OpenHands | `.agents/skills/` |
| Pi | `.agents/skills/` |
| Pochi | `.agents/skills/` |
| Qoder | `.agents/skills/` |
| Qwen | `.agents/skills/` |
| Roo Code | `.agents/skills/` |
| Sourcegraph | `.agents/skills/` |
| Trae | `.agents/skills/` |
| Vibe | `.agents/skills/` |
| VSCode | `.agents/skills/` |
| Windsurf | `.agents/skills/` |
| Zed | `.agents/skills/` |
| ZenCoder | `.agents/skills/` |

Most agents use `.agents/skills/` as their skill directory. Claude Code is the notable exception, using `.claude/skills/`.

The `npx skills add` command automatically detects which agents are installed and places skill files in the correct directory for each one.
